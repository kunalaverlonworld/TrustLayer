{
  "name": "trusttlayerr-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "docker:up": "docker-compose up -d",
    "start": "npm run backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
